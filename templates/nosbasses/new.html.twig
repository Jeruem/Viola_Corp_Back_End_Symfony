{% extends 'base.html.twig' %}

{% block title %}Create New Bass{% endblock %}

{% block body %}
    <h1>Create new Bass</h1>

    {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }} {# Ajoute l'attribut enctype ici #}
        {{ form_widget(form) }} {# Affiche tous les champs du formulaire #}
        <button type="submit">Save</button>
    {{ form_end(form) }}
    
    <a href="{{ path('app_nosbasses_index') }}">back to list</a>
{% endblock %}

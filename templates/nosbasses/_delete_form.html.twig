<form method="post" action="{{ path('app_nosbasses_delete', {'id': nosbass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nosbass.id) }}">
    <button class="btn">Delete</button>
</form>

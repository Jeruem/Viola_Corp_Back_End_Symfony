{% extends 'base.html.twig' %}

{% block title %}NosGuitares{% endblock %}

{% block body %}
    <h1>NosGuitares</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nos_guitare.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ nos_guitare.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ nos_guitare.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ nos_guitare.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nos_guitares_index') }}">back to list</a>

    <a href="{{ path('app_nos_guitares_edit', {'id': nos_guitare.id}) }}">edit</a>

    {{ include('nos_guitares/_delete_form.html.twig') }}
{% endblock %}
